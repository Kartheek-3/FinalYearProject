# Docker boundary

This folder is reserved for SEAM platform container definitions and deployment adapters. It intentionally contains no image build or execution configuration until the Delivery and deployment stages are implemented.

Generated-product Dockerfiles and Compose manifests must be created inside their individual `generated_projects/<project-id>/` workspaces, never here.
